"""Re-exported from test_integration_*.py split files."""
# Tests live in:
#   test_integration_graph.py    — concept packet in challenge node
#   test_integration_rechallenge.py — rechallenge uses app-selected concept
#   test_integration_no_ready.py — NoReadyConcept surfaces as HTTP 422
#   test_integration_db.py       — concept_id persists in DB rows (requires Postgres)
