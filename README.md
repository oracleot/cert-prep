# cert-prep

Phase 1 scaffold for the AI certification prep app.

## Quick start

1. Copy env vars:

```bash
cp .env.example .env.local
```

2. Install dependencies:

```bash
npm install
```

3. Start dev server:

```bash
npm run dev
```

The app runs at http://localhost:3000.
